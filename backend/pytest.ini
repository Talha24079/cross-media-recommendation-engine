[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -ra --strict-markers --cov=. --cov-report=term-missing --cov-fail-under=90
markers =
    unit: isolated tests for services and validation
    integration: API-level tests against the FastAPI app